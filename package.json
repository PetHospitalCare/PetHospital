{
  "name": "my-fullstack-app",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "cd ph_fe && npm install && npm run build",
    "start": "cd PH_BE && node server.js"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}